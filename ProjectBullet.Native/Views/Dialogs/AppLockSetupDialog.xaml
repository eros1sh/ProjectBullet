<Page x:Class="ProjectBullet.Native.Views.Dialogs.AppLockSetupDialog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d"
      d:DesignHeight="280" d:DesignWidth="400"
      MinWidth="350"
      Background="{DynamicResource BackgroundSecondary}"
      Title="Setup App Lock">

    <Border Padding="20">
        <StackPanel
            MaxWidth="400"
            HorizontalAlignment="Center">

            <TextBlock
                Text="Set App Lock Password"
                FontSize="16"
                FontWeight="Bold"
                TextAlignment="Center"
                HorizontalAlignment="Center"
                Foreground="Gainsboro"
                Margin="0 0 0 15" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 10">
                <Label Foreground="Gainsboro" Width="120">Password:</Label>
                <PasswordBox x:Name="passwordBox" Width="200" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 10">
                <Label Foreground="Gainsboro" Width="120">Confirm:</Label>
                <PasswordBox x:Name="confirmBox" Width="200" />
            </StackPanel>

            <TextBlock
                x:Name="errorText"
                Text=""
                Foreground="Red"
                FontSize="12"
                TextAlignment="Center"
                HorizontalAlignment="Center"
                Margin="0 0 0 10" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Click="Save"
                    Margin="0 5 5 5" Style="{StaticResource StyledSuccessButton}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconOcticons Kind="Check" />
                        <TextBlock VerticalAlignment="Center" Margin="5 0">Save</TextBlock>
                    </StackPanel>
                </Button>
                <Button Click="Cancel"
                    Margin="0 5 5 5" Style="{StaticResource StyledDangerButton}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Close" />
                        <TextBlock VerticalAlignment="Center" Margin="5 0">Cancel</TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>

        </StackPanel>
    </Border>

</Page>
